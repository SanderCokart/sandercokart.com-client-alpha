import {Swiper, SwiperSlide} from 'swiper/react';
import {Navigation, Scrollbar, Mousewheel} from 'swiper';

const Test = (props: {}) => {
    return (
        <>

        </>
    );
};

export default Test;